{
    "employees": [
    {{#each hits}}
            {
              "result": {{ _source.workers }},
              "name": "{{ _source.name }}"
            }{{#unless @last}},{{/unless}}
    {{/each}}
    ]
}
