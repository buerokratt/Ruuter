- step: get_message
  call: http.get
  args:
    url: https://example.com/endpoint
    query:
      some_val: "Hello World"
      another_val: 123
  result: the_response

- step: post_message
  call: http.post
  args:
    url: https://example.com/endpoint
    body:
      some_val: "Hello World"
      another_val: 123
  result: the_message

- step: assign
  call: assign
  assign:
    stringValue: "Bürokratt"
    integerValue: 2021

- step: return_value
  call: return
  return: ${the_message.body}
