application:
  config-path: ${user.dir}/services
  stopProcessingUnrespondingSteps: true
  logging:
    displayRequestContent: false
    displayResponseContent: false

spring:
  application:
    name: ruuter

logging:
  level:
    root: INFO
