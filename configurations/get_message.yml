- get_message:
    call: http.get
    args:
      url: {ruuter_url}/endpoint
      query:
        some_val: "Hello World"
        another_val: 123
    result: the_message

- return_value:
    return: ${the_message.body}
